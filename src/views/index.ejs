<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
    <title>
        <%= title %>
    </title>
</head>

<body>
    <div class="container">
        <h1 class="display-2 mt-4">Url Shrinker</h1>
        <form action="/shortUrl" method="post" class="my-4 row">
            <div class="col-md-6">
                <div class="input-group">
                    <span for="fullUrl" class="input-group-text">URL</span>
                    <input class="form-control" type="url" name="fullUrl" id="fullUrl">
                    <button type="submit" class="btn btn-primary">Shrink</button>
                </div>
            </div>
        </form>

        <% if (data.length !== 0) { %>
            <div class="row">
                <div class="col-md-6">
                    <table class="table table-responsive">

                        <thead>
                            <tr>
                                <th>Full URL</th>
                                <th>Short URL</th>
                                <th>Clicks</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% data.forEach(url=> { %>
                                <tr>
                                    <td><a href="<%= url.full %>">
                                            <%= url.full %>
                                        </a></td>
                                    <td><a href="/<%= url.short %>" target="_blank">
                                            <%= url.short %>
                                        </a></td>
                                    <td>
                                        <%= url.clicks %>
                                    </td>
                                </tr>
                                <% }) %>
                        </tbody>
                    </table>
                </div>
                <% } %>
            </div>
    </div>
</body>

</html>